# Buttons

```html
<button class="button primary">Save</button>
<button class="button secondary">Cancel</button>
<button class="button contrast">Continue</button>
<button class="button danger">Delete</button>
```

Modifiers include `outline`, `ghost`, `link`, `icon`, `block` and `loading`. Sizes are `sm`, `md`, `xl` and `xxl`.

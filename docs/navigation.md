# Navigation

Navigation includes navbar, dropdown, sidebar, breadcrumb and pagination.

Interactive navigation requires `lithe.js`. Triggers must reference targets with valid ID selectors and expose `aria-expanded`.

Keyboard support includes arrows, Home, End, Escape and focus restoration where applicable.

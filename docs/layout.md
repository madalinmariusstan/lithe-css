# Layout

Base layout primitives are `.container`, `.stack`, `.cluster`, `.row` and `.col-*`.

The Extended application shell uses `.app`, `.app-sidebar`, `.app-main`, `.app-header` and `.app-content`.
